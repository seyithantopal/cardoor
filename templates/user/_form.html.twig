{{ form_start(form) }}
{% form_theme form 'bootstrap_4_layout.html.twig' %}
<div class="status" style="display: none;">
    {{ form_widget(form.status, { 'attr': {'class': 'hidden-row'} }) }}
    {{ form_widget(form.roles, { 'attr': {'class': 'hidden-row'} }) }}
</div>
{{ form_widget(form) }}
<button class="btn btn-dark"><i class="ik ik-save"></i>{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
