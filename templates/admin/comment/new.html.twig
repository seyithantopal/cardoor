{% extends 'adminbase.html.twig' %}

{% set name = 'Create New Comment' %}
{% block title %}{{ name }}{% endblock %}
{% block nav %}Comment{% endblock %}
{% block sub_nav %}{{ name }}{% endblock %}

{% block body %}
<div class="card">
    <div class="card-header"><h1>{{ name }}</h1></div>
    <div class="card-body">

    {{ include('admin/comment/_form.html.twig', {'flag' : 0}) }}

        <br><a href="{{ path('admin_comment_index') }}"><button class="btn btn-dark"><i class="ik ik-arrow-left"></i>back to list</button></a>

    </div>
</div>
{% endblock %}
