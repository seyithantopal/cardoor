{% extends 'adminbase.html.twig' %}

{% set name = 'Image New Category' %}
{% block title %}{{ name }}{% endblock %}
{% block nav %}Image{% endblock %}
{% block sub_nav %}{{ name }}{% endblock %}

{% block body %}
    <div class="card">
        <div class="card-header"><h1>{{ name }}</h1></div>
        <div class="card-body">

            {{ include('admin/image/_form.html.twig', {'button_label' : 'Submit' })  }}

            <br><a href="{{ path('admin_image_index') }}"><button class="btn btn-dark"><i class="ik ik-arrow-left"></i>back to list</button></a>
        </div>
    </div>
{% endblock %}
